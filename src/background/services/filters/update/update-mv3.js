export class FilterUpdateService {
  // Dummy method, added for backward compatibility with other modules.
  static async checkForFiltersUpdates(_filterIds) {
    return [];
  }

  // Dummy method, added for backward compatibility with other modules.
  static async autoUpdateFilters(_forceUpdate) {
    return [];
  }
}
